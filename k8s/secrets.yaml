apiVersion: v1
kind: Secret
metadata:
  name: recruithub-secrets
  namespace: recruithub
type: Opaque
stringData:
  # MongoDB credentials (base64 encoded in production)
  MONGO_USERNAME: "admin"
  MONGO_PASSWORD: "changeme123"
  
  # JWT Secret
  JWT_SECRET: "your-secret-key-change-in-production"
  
  # MongoDB URL (constructed)
  MONGO_URL: "mongodb://admin:changeme123@mongodb-service:27017/"